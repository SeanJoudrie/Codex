// Step 4 (roadmap) — LLM pass: one line on the technique, plus 1–3 category ids.
// Not wired up yet. For each repo where technique === null:
//   input:  description, topics, first ~3k chars of README, data/categories.json
//   output: { technique: "Agent sense-rotate-move over a decaying trail texture",
//             categories: ["simulation"] }
// Keep categories from seeds/sources unless the model is confident they're wrong.
console.log('tag: not implemented yet — skipping');
